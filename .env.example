# Testing
TEST_EMAIL=test@example.com
TEST_PASSWORD=password123
BASE_URL=http://localhost:3000

# Playwright E2E helpers (local only)
# REQUIRED for running E2E tests. The internal E2E-only endpoints are gated by E2E_TEST_MODE=1.
# SECURITY: Generate a unique secret for your local environment (do NOT commit real secrets).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
E2E_INTERNAL_SECRET=REPLACE_WITH_RANDOM_SECRET

# AI Services (OpenRouter - unified billing)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxx

# Site identification for OpenRouter rankings (optional)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Super Admin — the single designated super_admin account (exactly one)
SUPER_ADMIN_EMAIL=dark7eaper@bearing.app

# Maintenance Mode (optional)
# Format: {"enabled": true, "message": "System maintenance in progress"}
NEXT_PUBLIC_MAINTENANCE_MODE=

# Marketplace subscription tier override (optional)
# Values: free | pro | enterprise
NEXT_PUBLIC_SUBSCRIPTION_TIER=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key for server-side operations (webhooks, admin functions)
# Get from Supabase Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe (for ISBN purchase and marketplace services)
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxx

# DEPRECATED (no longer needed after Story 3.4 OpenRouter migration):
# GEMINI_API_KEY=xxxx
# MODAL_LLAMA_URL=xxxx
# MODAL_API_TOKEN=xxxx
